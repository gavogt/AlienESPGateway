{
    "servers": {
        "AlienESPGateway": {
            "type": "stdio",
            "command": "npm",
            "args": ["run", "mcp:dev"],
            "env":{
                "OPENAI_API_KEY": "${env:OPENAI_API_KEY}"
            }
        }
    }
}